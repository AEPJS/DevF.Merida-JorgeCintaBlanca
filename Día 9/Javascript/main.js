/* Crea una función llammada suma que reciba dos parámetros, a y b, y regrese la suma de ambos números. 
Recuerda que para devolver el valor de una función se utiliza la palabra reservada "return" */

function suma (a, b){

    let resultado = a + b;
    console.log(resultado);

}

suma(4,7);